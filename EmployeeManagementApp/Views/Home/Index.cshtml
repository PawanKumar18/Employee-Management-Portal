@{
    ViewData["Title"] = "Home Page";
}

<div class="text-center">
    @if (User?.Identity != null && User.Identity.IsAuthenticated)
    {
        <ul class="navbar-nav">
            <li class="nav-item">
                <a class="btn btn-primary text-white" asp-controller="Employees" asp-action="Create" style="padding: 0.5rem 1rem;">
                    <i class="fas fa-user-plus"></i> Add Employee
                </a>
            </li>
            <li class="nav-item">
                <a class="btn btn-primary text-white" asp-controller="Employees" asp-action="Index" style="padding: 0.5rem 1rem;margin-top:20px">
                    <i class="fas fa-user-plus"></i> Edit Employee
                </a>
            </li>
        </ul>

    }
</div>